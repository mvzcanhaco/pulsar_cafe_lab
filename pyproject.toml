[project]
name = "pulsar-cafe-lab"
version = "0.1.0"
description = "Automação comercial para PDV Fiserv/Clover — servidor web Python"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
line-length = 100
target-version = "py311"
